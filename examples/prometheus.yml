global:
  scrape_interval: 15s

scrape_configs:
  - job_name: agentic-ops-assistant
    metrics_path: /metrics/prometheus
    authorization:
      type: Bearer
      credentials_file: /run/secrets/prometheus-scrape-token
    static_configs:
      - targets: ["host.docker.internal:8000"]
